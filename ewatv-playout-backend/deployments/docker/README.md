# Deprecated — use deployments/podman/

Docker Compose was replaced by **Podman Compose** for lower memory overhead and native secret/quadlet support on Hetzner AX42.

See: `deployments/podman/compose.yaml` and `deployments/podman/compose.dev.yaml`
