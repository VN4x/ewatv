# Mist API credentials (set in Mist web UI at http://localhost:4242 on first boot)
MIST_API_USER=admin
MIST_API_PASSWORD=changeme

# Shared secret: Lovable / ewatv server fn → playlist-sync POST
PLAYLIST_SYNC_TOKEN=dev-sync-token

# Host port mappings (optional)
MIST_API_HOST_PORT=4242
MIST_HTTP_HOST_PORT=8080
CADDY_HTTP_PORT=80
CADDY_HTTPS_PORT=443

# Caddy TLS domain (production)
# MIST_DOMAIN=tv.ewatv.com
